# DialogueEval WebUI 产品走查报告

走查时间：2026-05-17  
走查地址：http://127.0.0.1:8501  
走查方式：使用无头 Chrome 实际点击页面，上传 `Resource/eval_standards/scene_1_eval_standard.md`，开启 fake LLM，case 数量设为 1，发起一次完整评测并进入报告页。  
本次测试运行：`run_20260517_110314_generated_scene`  
截图目录：`/private/tmp/dialogueeval-audit`

## 1. 总体判断

当前 WebUI 已经能支撑一条完整演示链路：

1. 上传任务指令。
2. 生成测试设计资产。
3. 发起客服模型与用户模拟器对话。
4. 生成评测报告。
5. 从新建评测页点击“查看评测结果”进入对应报告详情。
6. 查看人工校准、数据沉淀、Prompt 管理、模型调用和服务状态。

整体上，它已经不是纯命令行工具，具备可演示形态。但从产品成熟度看，当前更像“工程后台”，还不像面向评测人员或业务同学的稳定产品。主要问题是：技术配置暴露较多、结果解释链路还不够直观、部分页面状态跳转不符合用户预期、报告阅读体验偏原始。

## 2. 已覆盖功能

- 工作台：查看服务状态、测试设计数量、运行记录、最近平均分和最近评测。
- 新建评测：上传 Markdown 任务文件、查看任务详情弹窗、设置 case 数量、开启 fake LLM、发起完整评测。
- 评测完成态：进度卡片展示完成状态，并出现“查看评测结果”按钮。
- 评测报告：查看总览、Case 明细、Markdown 报告、原始文件。
- 人工校准：查看对话记录、事实项标注表单、当前标注进度。
- 系统管理：查看测试设计资产、Prompt 管理、数据沉淀、模型调用、服务状态。
- 侧边栏：展开和收起均可用。

## 3. 主要问题

### P0：影响用户理解或核心闭环

1. 评测报告页导航状态有问题  
   当我从“查看评测结果”进入报告详情后，再点击左侧“评测报告”，页面仍停留在当前 run 的详情页，没有回到评测运行列表。用户会以为左侧导航失效。建议点击左侧“评测报告”时明确回到运行列表，或者保留详情但提供更明显的当前位置和返回逻辑。

2. 分数结论和覆盖结论容易矛盾  
   本次 fake run 显示总分 85、通过，但同时缺失 7 个覆盖检查点。普通用户会困惑：缺这么多为什么还能通过？建议把“评分通过”和“覆盖通过”拆开展示，并给出总判定，例如“评分通过，但覆盖不足，建议复测”。

3. 新建评测页暴露太多技术配置  
   业务配置路径、生成策略路径、模型配置路径、输出目录、fake LLM 都直接展示在主流程右侧。对演示对象或业务同学来说理解成本高。建议默认只展示“case 数量、模型选择、是否评分”，路径类配置放到高级折叠区或系统设置。

4. 运行记录名称仍可能泛化  
   这次 fake run 在报告详情中显示“评测任务”，资产中也出现“模型生成场景”“generated_scene”。这会让用户不知道自己看的到底是哪条任务。建议强制使用任务指令摘要作为 run 名称，兜底也不能显示 generated_scene 这类工程名。

5. 人工校准的运行下拉难区分  
   多条运行记录名称相似时，下拉里只看到类似“飞毛腿合同签署通知”，缺少时间、case 数、是否已标注等关键信息。建议下拉项展示“短名称 + 发起时间 + case 数 + 标注进度”。

### P1：影响效率和专业感

6. Markdown 报告没有真正阅读优化  
   报告页的 Markdown 报告仍以等宽文本块展示，表格、标题、列表没有被渲染成正式报告样式。建议改成 Markdown 渲染视图，并提供“复制 Markdown / 下载报告”按钮。

7. 原始文件页过于原始  
   原始 JSONL 直接铺满页面，缺少搜索、筛选、复制、格式化、按 case 跳转。建议默认展示结构化摘要，把原始内容放到“开发者查看”里。

8. Case 明细缺少证据链导航  
   当前能看到原始对话、分数、缺失项，但“缺失检查点 -> 对话轮次 -> 原始评测要求”的链路还不够强。建议每个缺失项都能展开看到：对应标准、judge 理由、相关对话轮次、人工复核入口。

9. 人工校准表单工作量仍偏大  
   右侧表单事实项很多，需要人工逐项判断。建议增加“采用系统判定预填”“只看未判断项”“跳到下一个未完成项”“从对话选取证据”的能力。

10. Prompt 管理里的 Base URL 卡片换行不自然  
   `http://phoenix:6006` 在卡片中被断成两行，观感不专业。建议这类长文本使用小字号、等宽文本、tooltip 或复制按钮，不要当作大数字指标展示。

11. 系统管理页更像开发者后台  
   测试设计资产、Prompt 管理、数据沉淀、模型调用、服务状态都很有用，但不应和普通评测流程平级。建议系统管理明确标注为“开发者 / 管理员”，普通演示默认隐藏。

12. 模型调用页缺少成本与失败信息汇总  
   当前能看到任务、角色、模型、耗时、输入输出字符、hash、成功状态，但缺少 token 成本、失败重试、trace 链接、按角色统计。建议增加顶部汇总和筛选。

### P2：视觉和交互细节

13. 工作台信息密度偏弱  
   工作台展示了指标和最近评测，但缺少可行动入口。建议最近评测行可以直接点击进入报告，指标卡可以展示趋势或解释。

14. 新建评测完成后按钮状态可再优化  
   完成后“开始完整评测”又恢复可点击，容易误触再次运行。建议完成态下主按钮改为“再次运行”或“新建下一次评测”，并加二次确认。

15. 任务详情弹窗阅读体验还可以继续优化  
   弹窗的毛玻璃和 Markdown 渲染已可用，但内容较长时缺少目录、搜索、关键任务摘要。建议顶部固定展示核心目标、变量替换结果、风险项摘要。

16. 数据沉淀页路径信息过长  
   Dataset 表格里的路径很长，容易挤占空间。建议只显示文件名或相对路径，完整路径放 tooltip 或复制按钮。

17. 服务状态页是原始 JSON  
   服务状态直接展示 JSON 对开发者友好，但对演示对象不友好。建议改成健康卡片：API、Registry、Tracing、Phoenix、Postgres、最近错误。

18. 侧边栏收起后可用，但主内容间距仍偏大  
   收起状态视觉上可用，按钮位置正常。后续可以进一步压缩左侧空白，让大屏下内容区更聚焦。

## 4. 建议的信息架构

建议把页面分成三类：

1. 业务主流程  
   - 工作台
   - 新建评测
   - 评测报告
   - 人工校准

2. 资产与证据  
   - 测试设计资产
   - 覆盖矩阵
   - Case card
   - 对话记录
   - 评分证据

3. 开发者后台  
   - Prompt 管理
   - Phoenix Trace
   - 数据沉淀
   - 模型调用
   - 服务状态

当前系统管理页已经包含第三类能力，但建议明确弱化它在普通用户视角中的存在感。

## 5. 推荐迭代顺序

### 第一阶段：让演示链路更顺

1. 修复点击左侧“评测报告”不回到列表的问题。
2. 新建评测完成后，将按钮区改成“查看报告 / 再跑一次 / 新建评测”三种明确动作。
3. 报告总览增加综合结论：评分是否通过、覆盖是否通过、是否建议人工复核。
4. 工作台最近评测支持点击进入报告详情。
5. 隐藏路径类高级配置，默认只保留业务用户能理解的配置。

### 第二阶段：让报告更可信

1. Markdown 报告渲染成正式报告样式。
2. 每个扣分项和缺失项增加证据链：任务要求、对话轮次、judge 理由。
3. Case 明细增加“只看失败项 / 只看风险项 / 只看 P0 缺失项”。
4. 报告页支持导出 Markdown、CSV、JSON。

### 第三阶段：让标注更高效

1. 人工校准表单支持系统判定预填。
2. 支持从对话中选中文本作为证据。
3. 增加“下一个未完成项”和“只显示未判断项”。
4. 运行下拉项展示时间、case 数、已标注数量。

### 第四阶段：开发者能力后台化

1. Prompt 管理、模型调用、服务状态移入开发者模式。
2. Prompt 同步、扫描 outputs 这类写操作增加确认和结果反馈。
3. 模型调用页增加成本、token、重试、失败原因、trace 链接。
4. 服务状态从 JSON 改成卡片化健康检查。

## 6. 结论

这套 WebUI 的核心闭环已经跑通，尤其是“发起评测 -> 生成报告 -> 从完成态进入报告详情”已经具备演示价值。下一步不应只继续美化，而应优先解决用户理解成本和结果可信度表达问题。

优先级最高的是：报告页状态逻辑、评分与覆盖结论拆分、高级配置降噪、报告证据链增强、人工校准效率提升。

