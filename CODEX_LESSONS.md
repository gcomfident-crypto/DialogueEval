# Codex 执行经验与避坑清单

本文记录 DialogueEval 项目推进过程中已经暴露过的问题、原因和规避方式。后续每次继续开发前，应先快速阅读本文件，尤其是“执行前检查清单”。

## 执行前检查清单

1. 先确认用户最新需求，不要被历史上下文里的旧目标带偏。
2. 修改前先用 `rg`、`sed`、`git diff` 看清现有实现，不要凭记忆改。
3. 不要硬编码客服话术、用户话术、场景资产、用户画像、评分结论；应由模型、配置或资产文件生成。
4. 尽量避免正则和脆弱模式匹配；能用结构化字段、文件路径、JSON/YAML、显式状态就不用文本猜测。
5. 前端状态要先判断是页面内状态、跨页面状态，还是刷新后持久状态；不要把不该持久化的东西写进 `localStorage`。
6. 长耗时流程不要用同步 HTTP 请求硬等；要用后台 job、进度接口和前端轮询。
7. 改 API 请求结构时，前端、后端、测试、Docker 服务都要同步更新。
8. 运行评测前确认是否需要真实 DeepSeek；真实模型调用要考虑超时、重试、成本和进度反馈。
9. 涉及 Phoenix trace 时，要确认 trace 是否真实来自 DeepSeek 调用，而不是 fake LLM 或空数据。
10. 清理运行记录时，要区分 `outputs/runs` 文件、SQLite registry、Phoenix/Postgres traces，不要只删一个地方就断言清理完成。
11. 不要主动清理或回滚用户/历史生成的无关文件；只处理和当前任务直接相关的文件。
12. 前端改动后要构建 Web，后端改动后要跑测试并重建 API；最后确认服务 URL 返回正常。

## 已出现过的问题与经验

### 1. 同步长请求导致前端无反馈或超时

问题：完整评测包含资产生成、客服模型、用户模型、judge、评分报告，多次 LLM 调用会让 `/evaluations` 请求长时间挂起。用户看到的是“Request timed out”或页面无响应。

规避：
- 长流程使用 `/evaluations/jobs` 创建后台任务。
- 前端轮询 `/evaluations/jobs/{job_id}`。
- 进度面板显示阶段、百分比、当前 case、当前 run。
- 不要把长流程只包在一个同步按钮里。

### 2. 切换左侧页面后进度丢失

问题：最初把 `evaluationProgress` 放在 `NewEvaluation` 页面内部，切到其他 tab 后组件卸载，进度状态和轮询都消失。

规避：
- 跨页面仍需保留的状态放到顶层 `Home`。
- 页面组件只负责展示和发起操作。
- 后台 job 轮询也应由顶层状态驱动。

### 3. 上传文件状态持久化粒度判断错误

问题：曾把上传 Excel 和解析出的任务列表写入 `localStorage`，导致刷新后仍然保留。后续用户明确要求刷新后清空，只保留页面内切换。

规避：
- 页面内切换保留：React 顶层 state。
- 刷新后保留：`localStorage` 或后端 session。
- 不要默认持久化用户刚上传的文件解析状态。

### 4. Excel 多任务没有选择能力

问题：Excel 解析出多条任务指令后，系统默认全量评测，用户不能只选择部分指令。

规避：
- 前端每条任务指令前加 checkbox。
- 默认全选，但允许取消。
- 请求中传 `selected_eval_standard_paths`。
- 后端重新提取 Excel 后按选中的临时 Markdown 路径过滤，确保未选任务不会生成资产和评测。

### 5. 评测资产与对话不能硬编码

问题：用户多次强调客服话术、用户话术、用户画像、场景卡片、评分内容都不应写死。

规避：
- 场景资产、用户画像、case card、评分规则由 LLM 生成，并保存为 YAML/JSON/MD 中间产物。
- 客服模型和用户模型必须基于同一段 conversation history 轮流生成。
- judge 读取完整 history 判覆盖。
- 不要用固定模板模拟真实用户/客服对话。

### 6. 资产生成不能简单解析原文后硬拼

问题：用户要求用户画像和场景卡片由模型生成，而不是直接解析旧文档字段后填模板。

规避：
- 原始任务指令只作为输入材料。
- 资产生成走 LangGraph 流程。
- 输出资产必须有结构化文件：`scene_asset.yaml`、`coverage_plan.yaml`、`user_profiles.yaml`、`case_cards.yaml`、`scoring_rubric.yaml` 等。

### 7. 同一任务指令不应重复生成多个场景资产

问题：同一美团外呼指令跑多次，曾出现多个语义重复但 ID 不同的资产目录。

规避：
- 场景 ID 应基于任务指令核心意图或输入 hash 做稳定归并。
- 后续再评估同一任务时复用或覆盖已有资产，而不是无脑新增目录。
- 展示层使用中文短名，不直接暴露 hash 或长 run_id。

### 8. 变量占位符要实例化

问题：任务模板里有 `X单Y天`、`Z.W多日` 等占位符，客服模型直接说占位符会让用户模型无法理解。

规避：
- 资产生成前先做任务标准实例化。
- 占位符替换为固定数字，并记录到 `variable_assignments.yaml`。
- 对话和评分都使用实例化后的任务标准。

### 9. 前端 UX 不能只堆功能

问题：多次出现页面卡片过大、按钮位置突兀、侧栏动画跳变、表单嵌套滚动、页面切换后状态异常。

规避：
- 工具型平台优先清晰、紧凑、稳定，不做营销页风格。
- 避免大页面和内部区域双重滚动。
- 侧栏、按钮、logo 动画要用稳定尺寸和 transition。
- 操作按钮的位置要符合用户心智，不要出现意义不明的“刷新数据”。

### 10. 人工标注不能让人主观随便打分

问题：人工直接给维度分会很主观，标注人不知道错一个点扣多少。

规避：
- 人工标注事实项，不直接填维度分。
- 系统根据评分量表自动汇总分数。
- 未核查项不能直接显示正式通过/失败，应显示“待核查”。
- 点击下一条前要检查必填事实项，定位并高亮未完成项。

### 11. 数据清理要查清楚来源

问题：用户要求删除 run 时，可能涉及多个位置：文件系统、SQLite registry、Phoenix/Postgres traces、前端缓存。

规避：
- 先查 `/runs` 和 `/registry/experiments`。
- 再查 `outputs/runs`。
- 再查 `outputs/dialogue_eval_registry.sqlite3` 的 `experiments`、`case_runs`、`llm_calls`。
- Phoenix 要查 Postgres 的 `traces`、`spans`。
- 删除前后都要汇报命中数量。

### 12. Docker 和本地构建环境不稳定

问题：本机没有 node，前端类型校验依赖 Docker；Docker Hub 偶尔出现 EOF；有时 docker socket 权限会失败。

规避：
- Web 改动用 `docker compose build --pull=false web`。
- API 改动用 `.venv/bin/python -m pytest` 和 `docker compose build --pull=false api`。
- 构建成功后 `docker compose up -d api web`。
- 如果 Docker 网络临时失败，优先重试 `--pull=false`。

### 13. 生成输出文件可能污染 git diff

问题：运行评测和生成资产会改动 `outputs/` 下大量文件，`git diff --check` 可能报生成资产的历史尾随空格。

规避：
- 代码质量检查可限定到本次改动文件。
- 不要为了通过 diff check 擅自清理用户或历史生成的资产文件。
- 最终说明中区分“代码检查通过”和“历史输出文件存在问题”。

### 14. LLM judge 的可信度不能只靠模型自评

问题：系统能出分不等于可信，用户质疑“用 AI 评估 AI”是合理的。

规避：
- 建立人工 gold set。
- 让人工标事实项，系统自动算分。
- 统计 judge 与人工一致率、重复运行稳定性、P0 检查点召回率、风险识别准确率。
- 用强模型/弱模型对照实验验证分数能否拉开。

### 15. 覆盖全面性不能只靠多跑 case 数

问题：用户希望每个场景跑 200 条，但如果只有 12 张 case card，多跑只会重复或截断，不会自然增加覆盖。

规避：
- 先生成覆盖分类和覆盖矩阵。
- case 数量来自覆盖矩阵分配，不是简单重复。
- 增加动态用户状态：耐心下降、信任变化、挂断意图、诈骗怀疑、题外话、拒绝、追问等。
- 用覆盖矩阵命中率和人工抽查来证明覆盖能力。

### 16. Phoenix 不是评测系统本体

问题：用户曾困惑 Phoenix 和 DialogueEval 的关系。

规避：
- DialogueEval 是业务评测系统，负责资产生成、对话模拟、评分、报告、人工标注。
- Phoenix 是观测和 Prompt 管理工具，负责 trace、prompt 版本、调用链查看。
- 不要把业务结果只沉淀在 Phoenix；DialogueEval 自己也要有 registry 和报告产物。

## 推荐执行流程

每次接到新需求时按这个顺序做：

1. 复述目标和影响面，确认是前端、后端、评测流程、数据清理还是文档。
2. 用 `rg` 查入口和现有实现。
3. 判断状态是否需要跨页面、跨刷新、跨服务持久化。
4. 判断是否涉及长任务；如果是，优先 job + progress。
5. 判断是否涉及 AI 生成内容；如果是，避免硬编码和正则猜测。
6. 小步改动，保持接口兼容。
7. 跑对应验证：
   - Python/API：`.venv/bin/python -m pytest`
   - Web：`docker compose build --pull=false web`
   - API：`docker compose build --pull=false api`
   - 服务：`docker compose up -d api web`
8. 最终回答说明改了什么、验证了什么、还剩什么风险。

